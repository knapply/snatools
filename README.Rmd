---
output: 
  github_document:
    html_preview: false
editor_options: 
  chunk_output_type: console
---

<!-- README.Rmd generates README.md. -->

```{r, echo = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE, 
  fig.align = "center",
  comment = "#>",
  fig.path = "man/figures/"
)

knitr::opts_chunk$set(echo = TRUE)
```


# snatools <a href="man/figures/logo.png"> <img src="man/figures/logo.png" align="right" height="45%" width="45%" href="man/figures/logo.png"/> </a>

[![](https://img.shields.io/badge/devel%20version-0.0.0.9-red.svg)](https://github.com/knapply/snatools)

An R toolkit to bridge graph classes and streamline network analytic workflows.

## Installation

```{r, eval=FALSE}
# Install {devtools} if you haven't already.
# install.packages("devtools")

require(devtools)

# Install {snatools} from GitHub.
install_github("knapply/snatools")

# Load {snatools}.
library(snatools)
```

```{r, echo=FALSE}
library(snatools)
```


## Development Test Results

```{r, echo=FALSE, message=FALSE, warning=FALSE}
library(tidyverse)
choke <- capture.output(res <- as_tibble(devtools::test()))

res %>% 
  group_by(file, context) %>% 
  mutate(test = row_number()) %>% 
  summarise(`# of tests` = n(),
            `All Successful` = all(failed == 0L)) %>% 
  ungroup() %>% 
  select(-file) %>% 
  knitr::kable()
```


