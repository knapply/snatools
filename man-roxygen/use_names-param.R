#' @param use_names `logical` (default: `TRUE`) indicating whether to use vertex names as
#' the values filling the returned object. If `FALSE`, or a relevant attribute does not 
#' exist, the returned object uses vertex indices.
